# Remote Runner v0.5.3

## 修复

- **移除窗口最外层滚动条**：`html/body/#root` 外层容器禁止滚动，内容溢出时窗口边缘不再闪现滚动条；工作区、历史、编辑器、控制台等各面板内部的滚动行为保持不变。

## 验证范围

- 前端测试（58 项）、TypeScript 检查、Vite 构建通过。

## 升级说明

- 安装包可通过应用内「更新」按钮直接升级；便携版请到发布页下载 `Remote.Runner_0.5.3_x64-portable.exe`。
- `latest.json` 与 `.exe.sig` 用于更新校验，无需手动打开。
